{% trans_default_domain "filters" %}

{% macro printAggregation(
    search_url,
    search_query,
    result,
    aggregationName,
    aggregationValueTransFormat,
    aggregationHeaderTransFormat
) %}
    {% import _self as _aggregations %}
    {% set aggregation = result.aggregation(aggregationName) %}
    {% if aggregation != null and not aggregation.isEmpty %}
        {{ aggregation.sortByName }}
        {{ _aggregations.printTitle(aggregationName, aggregationHeaderTransFormat) }}
        <ul>
            {% if aggregation.activeElements is not empty %}
                <li>
                    <a href="{{ url(search_url, search_query|remove_filter_value(aggregationName)) }}">
                        <span class="glyphicon glyphicon-remove-circle fa fa-remove"></span> {{ "labels.remove_selected"|trans }}
                    </a>
                </li>
            {% endif %}

            {% if aggregation.hasLevels %}
                {% for activeElement in aggregation.activeElements %}
                    <li>
                        <a href="{{ url(search_url, search_query|remove_filter_value(aggregationName, activeElement.id)) }}">
                            <span class="glyphicon glyphicon-remove-circle fa fa-remove"></span> {{ activeElement.name|trans }}
                        </a>
                    </li>
                {% endfor %}
            {% endif %}

            {% for name,counter in aggregation  %}
                <li>
                    {% set edited_search_query = counter.used
                        ? search_query|remove_filter_value(aggregationName, counter.id)
                        : search_query|add_filter_value(aggregationName, counter.id)
                    %}
                    {% set url = url(search_url, edited_search_query) %}

                    <a href="{{ url }}">
                        {% if not aggregation.hasLevels %}
                            <input type="checkbox"{% if counter.isUsed %} checked{% endif %} onclick='window.location.assign("{{ url }}")'>
                        {% endif %}
                        {{ _aggregations.printOptionValue(aggregationName, counter, aggregationValueTransFormat) }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
{% endmacro printAggregation %}


{% macro printPriceRange(
    search_url,
    search_query,
    result,
    aggregationValueTransFormat,
    aggregationHeaderTransFormat
) %}
    {% import _self as _aggregations %}
    {% set aggregation = result.aggregation('price') %}
    {% set result_min_price = result.minPrice %}
    {% set result_max_price = result.maxPrice %}

    <div id="price_range_wrapper"
        data-url-placeholder = "{{ url(search_url, search_query|remove_price_range_filter) }}"
        data-slider-max = "{{ result_max_price }}"
    >
        {{ _aggregations.printTitle('price', aggregationHeaderTransFormat) }}
        {% if app.request.query.has('price') %}
            <div>
                <a href="{{ url(search_url, search_query|remove_price_range_filter) }}">
                    <span class="glyphicon glyphicon-remove-circle fa fa-remove"></span> {{ "labels.remove_selected"|trans }}
                </a>
            </div>
        {% endif %}
        <input
            id="price_range"
            type="text"
            class="span2"
            value=""
            data-slider-min="{{ result_min_price }}"
            data-slider-max="{{ result_max_price }}"
            data-slider-step="1"
            data-slider-value="[{{ result_min_price }},{{ result_max_price }}]"
        />
    </div>

{% endmacro printPriceRange %}

{% macro printMultiplePriceRange(
    search_url,
    search_query,
    result,
    aggregationValueTransFormat,
    aggregationHeaderTransFormat
) %}

    {% import _self as _aggregations %}
    {% set aggregation = result.aggregation('price') %}
    {{ _aggregations.printTitle('price', aggregationHeaderTransFormat) }}

    <ul>
        {% if aggregation.activeElements is not empty %}
            <li>
                <a href="{{ url(search_url, search_query|remove_filter_value('price')) }}">
                    <span class="glyphicon glyphicon-remove-circle fa fa-remove"></span> {{ "labels.remove_selected"|trans }}
                </a>
            </li>
        {% endif %}

        {% for name,counter in aggregation  %}
            <li>
                {% set edited_search_query = counter.used
                    ? search_query|remove_filter_value('price', counter.id)
                    : search_query|add_filter_value('price', counter.id)
                %}

                <a href="{{ url(search_url, edited_search_query) }}">
                    <input type="checkbox"{% if counter.isUsed %} checked{% endif %}/>
                    {{ _aggregations.printOptionValue('price', counter, aggregationValueTransFormat) }}
                </a>
            </li>
        {% endfor %}
    </ul>

{% endmacro printMultiplePriceRange %}


{% macro printRatingRange(
    search_url,
    search_query,
    result,
    aggregationValueTransFormat,
    aggregationHeaderTransFormat
) %}
    {% import _self as _aggregations %}
    {% set aggregation = result.aggregation('rating') %}
    {{ _aggregations.printTitle('rating', aggregationHeaderTransFormat) }}

    <ul>

        {% if aggregation.activeElements is not empty %}
            <li>
                <a href="{{ url(search_url, search_query|remove_filter_value('rating')) }}">
                    <span class="glyphicon glyphicon-remove-circle fa fa-times"></span> {{ "labels.remove_selected"|trans }}
                </a>
            </li>
        {% endif %}

        {% for name,counter in aggregation  %}
            {% set elems = counter.name|split('..') %}
            <li>
                {% set edited_search_query = counter.used
                    ? search_query|remove_filter_value('rating', counter.id)
                    : search_query|add_filter_value('rating', counter.id)
                %}

                <a href="{{ url(search_url, edited_search_query) }}">
                    <input type="checkbox"{% if counter.isUsed %} checked{% endif %}/>
                    <span style="font-size: 12px; color: #337ab7;">
                        {% set numStars = (elems.0|number_format) %}
                        {% set numEmptyStarts = 10-numStars %}
                        {% if numStars > 0 %}
                            {% for i in 1..numStars %}
                                <span class="glyphicon glyphicon-star fa fa-star" aria-hidden="true"></span>
                            {% endfor %}
                        {% endif %}
                        {% if numEmptyStarts > 0 %}
                            {% for i in 1..numEmptyStarts %}
                                <span class="glyphicon glyphicon-star-empty fa fa-star-o" aria-hidden="true"></span>
                            {% endfor %}
                        {% endif %}
                    </span>
                    ({{ counter.n }})
                </a>
            </li>
        {% endfor %}
    </ul>
{% endmacro printRatingRange %}


{% macro printTitle(aggregationName, aggregationHeaderTransFormat) %}
    <h4>
        {% set aggregationHeader = aggregationHeaderTransFormat
            |default('fields.{{ field }}.label')
            |replace({
                '{{ field }}': aggregationName
            })
            |trans
        %}
        {{ aggregationHeader }}
    </h4>
{% endmacro printTitle %}


{% macro printOptionValue(aggregationName, counter, aggregationValueTransFormat) %}
    {% set aggregationValue = aggregationValueTransFormat
        |default('fields.{{ field }}.options.{{ option }}')
        |replace({
            '{{ field }}': aggregationName,
            '{{ option }}': counter.name|replace({'.': '~'})
        })
        |trans
    %}
    {{ aggregationValue }} ({{ counter.n }})
{% endmacro printOptionValue %}
