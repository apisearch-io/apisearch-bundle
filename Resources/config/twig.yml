services:

    #
    # Repositories
    #
    search_bundle.url_builder_extension:
        class: Puntmig\Search\Twig\UrlBuilderExtension
        arguments:
            - "@search_bundle.url_builder"
        tags:
            - { name: twig.extension }
